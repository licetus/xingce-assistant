# ============================================
# 微信小程序 · Git 忽略规则
# ============================================

# -------------------------------------------
# 1. 开发者工具私有配置【最重要，必须忽略】
# -------------------------------------------
# 打开开发者工具后会自动生成这个文件，存放个人本地设置
# （调试器布局、最近打开的文件、个人编译条件等）。
# 微信的加载优先级是：project.private.config.json > project.config.json
# 如果不忽略它，每次有人打开项目就会改动并被提交，团队天天解冲突。
project.private.config.json

# -------------------------------------------
# 1b. 本地环境配置【必须忽略】
# -------------------------------------------
# 存放真实云环境 ID。模板文件 config.example.js 是入库的，
# 复制它为 config.js 后填入真实值，这个文件永远不会被提交。
miniprogram/config.js

# -------------------------------------------
# 2. 依赖
# -------------------------------------------
node_modules/
miniprogram/node_modules/
cloudfunctions/*/node_modules/

# 云函数依赖在「上传并部署」时选择「云端安装依赖」即可，
# 本地 node_modules 不需要提交，也不应该提交（体积大且平台相关）
package-lock.json
yarn.lock
pnpm-lock.yaml

# -------------------------------------------
# 3. 构建产物
# -------------------------------------------
# 开发者工具「构建 npm」生成的目录，本地重新构建即可
miniprogram/miniprogram_npm/

# -------------------------------------------
# 4. 系统文件
# -------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# -------------------------------------------
# 5. 编辑器
# -------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~

# -------------------------------------------
# 6. 日志与临时文件
# -------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# -------------------------------------------
# 7. 本地环境配置（如后续引入）
# -------------------------------------------
.env.local
.env.*.local

# -------------------------------------------
# 8. 头像与设计素材（本地工作产物，不入库）
# -------------------------------------------
# 微信小程序头像最终是传到「微信公众平台 → 设置 → 小程序头像」，
# 不是从仓库里读。这两个目录都是生成过程的中间产物。
avatar/
assets/avatar-*.png
assets/xingce-avatar.png
assets/avatar-candidates/

# -------------------------------------------
# 9. 题库抓取管线的工作产物（可再生，不入库）
# -------------------------------------------
# fetch-gkzhenti.js / merge-fetched.js / to-mcp-format.js 的输出。
# 重新生成：npm run fetch:all && node scripts/merge-fetched.js
database/raw/
database/import/batches/
database/import/fetched-all.csv
