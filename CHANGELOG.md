# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)，
版本号与 Git tag 一致，格式 `MAJOR.MINOR.PATCH`。

日志文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 规范。

---

## [未发布]

### 已就绪

- 小程序工程骨架：5 主包页 + pkg-rank 分包（打卡/排行榜/战报海报）
- 核心闭环：专项刷题 → 即时解析 → 错题本 → 收藏
- 每日打卡：日历、连续天数、订阅消息提醒
- 排行榜：周榜 / 总榜，云端缓存不实时聚合
- 分享裂变：Canvas 2D 战报海报 + 带邀请参数的小程序码
- 云函数 9 个：login / question / answer / checkin / wrongbook / favorite / rank / share / timer / track
- 数据模型与 16 条索引定义
- 题库导入脚本（清洗、校验、去重、切片）
- 版本更新机制：静默更新 + 强制更新 + 柔性提示

### 待补

- tabBar 图标（当前为纯文字 tabBar，微信支持）
- 榜单「击败百分比」当前为本地估算，V1.1 应换成云端真实分位

---

## 版本说明

### 版本号怎么定

| 变更类型 | 版本段 | 例子 |
|---|---|---|
| 不兼容变更（云函数接口改契约、数据结构破坏性调整） | MAJOR | `1.0.0` → `2.0.0` |
| 新增功能，向下兼容 | MINOR | `1.0.0` → `1.1.0` |
| Bug 修复、文案调整 | PATCH | `1.0.0` → `1.0.1` |

### 发版时同步更新的地方

打 tag 后，以下三处要保持与 tag 一致：

1. Git tag：`git tag -a v1.1.0 -m "..."`
2. 微信后台版本号：开发者工具「上传」时填写
3. `config` 集合的 `latest_version`

三者对不上，出事故排查时会浪费大量时间。

---

## 模板

新增版本时，在「未发布」上方插入：

```markdown
## [1.1.0] - 2026-09-15

### 新增
-

### 修复
-

### 变更
-

### 兼容性说明
- 云函数 `xxx` 新增字段 `yyy`，老版本小程序不受影响
- 云函数 `aaa` 的字段 `bbb` 已标记废弃，将在 v1.3.0 移除
```
