# 行测小助手 · 项目总览与进度

> 最近更新：2026-09-06（云开发部署完成）｜ 分支：develop ｜ 最新提交：`acbf618`
> 技术栈：原生小程序 + 微信云开发 ｜ 主体：企业 ｜ 题库：自研
> 目标：4 周内提审上线，首月以「刷题留存 + 战报裂变」双轮获客

---

## 当前进度：W1 完成，云开发已部署 ✅

备案进行中，开发同步推进。**云开发环境已于 09-06 部署完毕并冒烟通过。**

| 阶段 | 事项 | 状态 |
|---|---|---|
| 基线 | 工程从 `xingce-assistant_副本` 迁回工作区（10 提交、双分支完整） | ✅ |
| W1 | 架构设计 v2.0 文档 | ✅ |
| W1 | Tab 结构改造 + 主色统一 + 设计令牌 | ✅ |
| W1 | 题库页 + 云函数接口（moduleStats / subtypes） | ✅ |
| W1 | 本地测试环境：134 用例全绿，修复 9 项缺陷（含 2 项致命/严重） | ✅ |
| W1 | **云开发部署：11 函数 + 3 定时触发器 + 11 集合 + 16 索引 + 权限 + 初始数据** | ✅ |
| W1 | **填入云环境 ID**（config.js + cloudbaserc.json） | ✅ |
| W1 | 部署冒烟：又抓出并修复 1 项严重缺陷（rank 聚合累加器误用） | ✅ |
| W1 | **填入微信 AppID**（`project.config.json`） | ⬜ 需你操作 |
| W2 | 首页按新 UI 稿重做（今日目标深色卡、专项 6 宫格） | ⬜ |
| W2 | 其余页面套用设计令牌 | ⬜ |
| W2 | 导入正式题库（当前云端仅 6 道样例题） | ⬜ |
| W3 | 打卡 / 排行榜 / 战报海报联调 | ⬜ |
| W3 | 隐私授权（`wx.getPrivacySetting` + 能力降级） | ⬜ |
| W4 | 埋点校验、审核自检、`audit_mode` 验证、提审 | ⬜ |

---

## 云开发环境（2026-09-06 部署）

| 项 | 值 |
|---|---|
| EnvId | `datizhushou-d7gmes5l22e316f09`（上海，别名 datizhushou） |
| 套餐 | **体验版**，有效期至 2027-03-06，QPS 500 |
| 云函数 | 11 个全部 Active（Nodejs18.15） |
| 定时触发器 | dailyTask 00:05 / rebuildRank 每小时:10 / archive 03:30 |
| 集合 | 11 个，权限全部「仅管理端可读写」 |
| 索引 | 16 条（users 默认非唯一索引已替换为唯一索引） |
| 初始数据 | config 5 条 + 样例题 6 道（qid 10001~10006） |

冒烟验证：`timer dailyTask` 抽题写库 ✓、`rank rebuild` 聚合写 rank_cache ✓
（修复 `_.sum` 误用后）、无身份直调返回 401 属预期安全行为。
环境 ID 在 `miniprogram/config.js`（已 gitignore，不入公开仓库）。

> ⚠️ **套餐提醒**：当前是体验版，正式运营前须升级**标准版**——
> 体验版资源与并发受限，数据量上来后 `answer` 判题事务与 `timer` 榜单重建可能超时。
> 体验版可用于当前联调阶段，不阻塞开发。

---

## 部署冒烟抓出的缺陷（已修复，commit `acbf618`）

**rank 云函数聚合累加器误用**：代码写 `_.sum()`（`db.command.sum`），但真实
wx-server-sdk 中聚合累加器位于 `db.command.aggregate` 命名空间，线上直接抛
「`_.sum is not a function`」——榜单重建与实时降级聚合全部不可用。
本地 mock 层曾把 sum 挂在顶层 command 宽容放行，掩盖了缺陷。
已改为 `$.sum` 并重新部署验证通过；mock 层同步改为真实 SDK 结构，
这类误用今后本地测试即可暴露。

另修复：login 测试的 mock 时钟改为跟随真实时钟（消除墙钟漂移偶发失败）。

---

## 需要你操作的事

1. **填微信 AppID**：`project.config.json` 的 `appid`（`touristappid` → 真实 AppID）。
   没有它无法在开发者工具里预览真机链路。
2. **体验版 → 标准版**：正式运营前升级（见上方提醒）。
3. **导入正式题库**：跑 `node scripts/import-questions.js <csv>` 产出 JSON Lines
   后导入 questions 集合（当前仅 6 道样例题）。

## 关键决策（已锁定，勿重复讨论）

| 项 | 结论 |
|---|---|
| 开发框架 | 原生小程序，不用 Taro/uni-app |
| 后端 | 微信云开发标准版 |
| V1 范围 | 不含限时模考，`pkg-exam` 分包位留给 V1.1 |
| Tab | 首页 / 题库 / 错题本 / 我的 |
| 判题 | 全在云端，下发题目不含 `answer` |
| 数据访问 | 全部走云函数，集合一律「仅管理端可读写」 |
| 状态管理 | `globalData` + 自研事件总线，不引 mobx |

---

## 上线关键路径

**备案（3~5 周）比开发（4 周）+ 提审（1 周）还长。** 上线时间由「第 1 天有没有提交备案」
决定，不由代码决定。备案已启动，开发必须跟上，否则备案下来了代码没好就白等。

备案流程里最容易翻车的一环：平台初审后会发**工信部 12381 短信核验**，
须在 **24 小时内**完成，超时自动驳回、申请作废需重来。安排专人盯这个短信。

---

## 主要文档

| 文件 | 内容 |
|---|---|
| `docs/架构设计.md` | 13 章完整架构 + 2 附录（**唯一权威架构文档**） |
| `docs/架构设计-v1.0-存档.md` | 初版，仅存档，勿参照 |
| `docs/上线提审清单.md` | 备案、类目、隐私指引、后台配置、时间线 |
| `docs/版本管理规范.md` | 分支策略、三套版本体系、云函数灰度 |
| `database/indexes.md` | 16 条索引清单 |
| `database/schema.md` | 集合字段结构 |
| `database/import/导入说明.md` | 建库四步 + JSON Lines 格式要求 |
