# 行测小助手 · 架构设计交付清单

> 交付日期：2026-08-29 ｜ 技术栈：原生小程序 + 微信云开发

## 本次交付了什么

一套**可直接导入微信开发者工具运行**的小程序工程骨架，加一份讲清楚每个决策理由的架构文档。

| 类别 | 内容 | 状态 |
|---|---|---|
| 架构文档 | `docs/架构设计.md` — 11 章，覆盖选型、分包、数据模型、云函数、性能、增长、合规、上线计划、风险 | ✅ |
| 小程序端 | 5 个主包页面 + 1 个分包（3 页）+ 题卡组件 + 6 个 utils + 5 个 services | ✅ 骨架完整 |
| 云函数 | 9 个：login / question / answer / checkin / wrongbook / favorite / rank / share / timer / track | ✅ 业务逻辑完整 |
| 数据模型 | `database/schema.md` 集合结构 + `database/indexes.md` 16 条索引 | ✅ |
| 题库导入 | `scripts/import-questions.js` 校验清洗切片，已用样例数据验证通过 | ✅ 可用 |
| 质量检查 | 全部 JS 通过 `node --check`，全部 JSON 通过解析 | ✅ |

合计 80 个文件、约 4400 行代码。

---

## 五个关键决策及理由

**1. 云开发而非自建后端**
砍掉「买服务器 → 买域名 → ICP 备案后端 → 申请 SSL → 配 request 合法域名 → 实现登录态」整条链路，上线周期压缩 2~3 周。日活 5 万以内性能无忧，`utils/cloud.js` 做了单一出口隔离，将来迁移只改一个文件。

**2. 答案不下发，判题全在云端**
客户端拿到的题目不含 `answer` 字段，无法本地判题，也就无法绕过云函数拖库。题库是这个产品唯一的资产，必须从源头保护。

**3. 「即时解析」与「答案不下发」不冲突**
需要即时解析时采用逐题提交（`instant` 模式）：答案在用户**作答之后**才下发，此时泄露已无意义。打卡和模考用 `batch` 模式统一提交。`answer` 云函数按 `items` 数组长度自动适配。

**4. 错题本列表放主包而非分包**
`tabBar.list[].pagePath` 必须指向主包页面。四个 tab 都得在主包，错题列表本身很轻（约 30KB），换完整的底部导航体验，值。

**5. 事务只包住「必须强一致」的部分**
20 题全量写入会产生 60+ 次操作，容易撞事务超时。只有错题本和用户统计进事务；`records` 是只增日志、`questions.stat` 只用于难度校准，两者放事务外异步写，允许最终一致。

---

## 增长设计（三条主线）

1. **战报海报裂变** — 答完题生成 Canvas 2D 海报 → 存相册 → 发朋友圈 → 好友扫码。小程序码 `scene` 带邀请人 openid 做归因
2. **打卡订阅消息** — 在打卡**完成后**申请授权（此时意愿最高，授权率 40~60%，远高于冷启动弹窗的 5%）
3. **自建好友关系** — 微信 `getFriendCloudStorage` 等关系链能力已收紧，非游戏类目基本拿不到好友数据。改为分享链接绑定邀请关系，排行榜只展示「我邀请的人」

---

## 下一步：拿到代码后必做的 6 件事

1. `project.config.json` 的 `appid` 从 `touristappid` 换成真实 AppID
2. `miniprogram/app.js` 的 `wx.cloud.init({ env })` 换成云环境 ID
3. 按 `database/schema.md` 建集合，**安全规则全部设为「仅管理端可读写」**
4. 按 `database/indexes.md` 建 16 条索引（其中 2 条唯一索引承担业务约束，不能省）
5. `config` 集合写入初始配置：`audit_mode`、`checkin_tmpl_id`、`share_text`
6. 用 `node scripts/import-questions.js <你的题库.csv>` 导入题库，再导入 `database/import/` 下的产物

---

## 需要你们决策的三件事

| 事项 | 说明 | 建议 |
|---|---|---|
| **ICP 备案** | 2024 年起小程序上架必须备案，企业主体 3~20 个工作日。**这是关键路径，必须第一天启动** | 立即启动 |
| **类目选择** | 「教育 > 在线教育」还是「工具 > 效率」 | 先选工具类目降低门槛，稳定后再加教育类目 |
| **题库版权** | 真题原文与官方解析不能直接用 | 全部自研或深度改编，保留创作记录 |

---

## 已知待补项

- tabBar 图标：当前是纯文字 tabBar（微信支持），补 81×81 PNG 后加 `iconPath`
- 榜单「击败百分比」：当前是本地按正确率估算的分位文案，V1.1 应换成云端真实分位
- `pkg-exam`（限时模考）分包：按要求不进 V1，已预留位置

默认头像占位图已生成（`miniprogram/assets/default-avatar.png`，96×96 纯代码生成，无外部依赖），可直接替换。
