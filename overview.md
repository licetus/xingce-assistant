# 行测小助手 · 架构设计 v2.0 交付说明

> 交付日期：2026-08-31 ｜ 交付形式：**仅文档，未改动任何代码**
> 输入：v1.0 架构（2026-08-29 已产出工程骨架）+ 2026-08-31 定稿的 UI 设计稿（5 屏 Apple 风格）

---

## 本次交付

| 文件 | 内容 |
|---|---|
| `docs/架构设计.md` | 13 章完整架构设计 + 2 个附录 |
| `overview.md` | 本文档 |

文档覆盖：范围锁定、技术选型、分包与信息架构、设计令牌、数据模型（10 集合 / 16 索引）、云函数设计（10 个）、关键链路时序、性能、增长、合规审核、上线计划、风险清单、演进预留。

---

## 两个已确认的范围决策

1. **只出文档，不动代码** — 现有工程 `~/WorkBuddy/xingce-assistant_副本` 未被触碰
2. **V1 不做限时模考** — `pkg-exam` 分包位保留给 V1.1

---

## 五个核心决策

**1. 云开发必须买标准版，个人版不可用**
个人版云函数超时锁死 3s、内存锁死 256MB，判题事务（批量写 records + 错题本 + 统计）必然超时。这是采购决策，不是代码问题，但会直接决定系统能不能跑起来。

**2. 答案不下发，判题全在云端**
客户端拿到的题目不含 `answer` 字段，无法本地判题，也就无法绕过云函数拖库。题库是唯一资产，从源头保护。「即时解析」不受影响——答案是用户**作答之后**才下发的。

**3. Tab 改为「首页 / 题库 / 错题本 / 我的」**
UI 稿原为「首页 / 题库 / 模考 / 我的」。模考不做，第 3 个 Tab 让给错题本（刷题工具的第二高频入口）。V1.1 模考上线时，错题本收进「我的」，主包结构不变。

**4. 事务只包住必须强一致的部分**
20 题全量写入会产生 60+ 次操作，容易撞事务超时。只有错题本和用户统计进事务；`records` 是只增日志、`questions.stat` 只用于难度校准，两者事务外异步写。

**5. 备案是关键路径，不是开发**
开发 4 周 + 提审 1 周 ≈ 5 周；首次主体 ICP 备案 3~5 周。上线时间由「第 1 天有没有提交备案」决定，不由代码决定。

---

## 本次核实出的三处规则变更（v1.0 文档已过时）

| # | 项 | v1.0 写法 | 2026 实际情况 |
|---|---|---|---|
| 1 | 分包总上限 | 20MB | **30MB**（服务商代开发的小程序为 20MB）；单包仍为 2MB |
| 2 | 云开发套餐 | 只写「超时可达 900s」 | **个人版锁死 3s / 256MB 不可改**，标准版起才可调整 |
| 3 | 备案流程 | 未提短信核验 | 平台初审后有**工信部短信核验（12381）**，须在 **24 小时内**完成，超时自动驳回、申请作废 |

另核实确认：`limit` 在小程序端最大 20、云函数端最大 1000；`wx.getUserProfile` 已回收（2.27.1+ 只返回「微信用户」+ 灰色头像），头像昵称必须走 `chooseAvatar` + `input type="nickname"`。

---

## 需要你们拍板的三件事

| 事项 | 说明 | 建议 |
|---|---|---|
| **ICP 备案** | 首次主体 3~5 周，已有主体备案 2~3 周。**第 1 天启动**，并安排专人盯 12381 短信 | 立即启动 |
| **云开发套餐** | 标准版起，个人版不可用 | 开工即采购 |
| **类目选择** | 「工具 > 效率」门槛低、「教育 > 在线教育」更贴合 | 先工具类目上线，稳定后追加教育类目 |

---

## 下一步：落地时按附录执行

`docs/架构设计.md` 的**附录 A** 列了 v1.0 → v2.0 的 10 项差异（Tab 结构、新增题库页、主色 `#2B6CF6` → `#0066CC`、新增设计令牌文件、组件调整、隐私授权、埋点事件等），**附录 B** 列了开工前必配的 8 项。

其中有两项会直接卡住开发，务必先做：
1. `project.config.json` 的 `appid` 换成真实 AppID
2. `app.js` 的 `wx.cloud.init({ env })` 换成云环境 ID

---

## 已知待补项

- tabBar 图标颜色需按新主色 `#0066CC` / `#9A9EA6` 重新生成（现有脚本 `scripts/gen-tabbar-icons.py` 产出的是旧的 `#2B6CF6`）
- 隐私授权 `wx.getPrivacySetting` 需基础库 2.32.3+，最低基础库定在 2.25.0 时必须做能力检测降级
- 云开发标准版日志仅保留 3 天，业务关键日志必须落库（已设计 `track` 云函数）
- 榜单「击败百分比」建议 V1.1 换成云端真实分位（当前方案为本地按正确率估算）
