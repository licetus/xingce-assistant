# 行测小助手 · 项目总览与进度

> 最近更新：2026-09-05 ｜ 分支：develop ｜ 最新提交：`0833a30`
> 技术栈：原生小程序 + 微信云开发（**标准版**）｜ 主体：企业 ｜ 题库：自研
> 目标：4 周内提审上线，首月以「刷题留存 + 战报裂变」双轮获客

---

## 当前进度：W1 已过大半

备案已启动（3~5 周），开发同步推进。

| 阶段 | 事项 | 状态 |
|---|---|---|
| 基线 | 工程从 `xingce-assistant_副本` 迁回工作区（10 提交、双分支完整） | ✅ |
| W1 | 架构设计 v2.0 文档 | ✅ |
| W1 | Tab 结构改造 + 主色统一 + 设计令牌 | ✅ |
| W1 | 题库页 + 云函数接口（moduleStats / subtypes） | ✅ |
| W1 | 题库导入格式修复 + config 初始数据 + 建库说明 | ✅ |
| W1 | **填入 AppID 与云环境 ID** | ⬜ 需你操作 |
| W1 | **建库：10 集合 + 安全规则 + 16 索引 + 导入题库** | ⬜ 需你操作 |
| W2 | 首页按新 UI 稿重做（今日目标深色卡、专项 6 宫格） | ⬜ |
| W2 | 其余页面套用设计令牌 | ⬜ |
| W3 | 打卡 / 排行榜 / 战报海报联调 | ⬜ |
| W3 | 隐私授权（`wx.getPrivacySetting` + 能力降级） | ⬜ |
| W4 | 埋点校验、审核自检、`audit_mode` 验证、提审 | ⬜ |

---

## 本次修复的两个真实缺陷

这两个不修工程跑不起来，但它们潜伏在「看起来很完整」的代码里：

**1. 主链路跳转全线失效**
`pages/practice/practice` 原本在 `tabBar.list` 里，但首页和错题本都用 `wx.navigateTo` 跳它。
`navigateTo` **不能跳 tabBar 页面**，运行时直接失败——首页点模块、打卡、错题重做、单题练习
四条路径全是断的。v2.0 把 practice 移出 tabBar 后全部恢复。

**2. 题库导入文件导不进去**
`scripts/import-questions.js` 输出的是格式化 JSON 数组，而云开发控制台要求
**JSON Lines**（每行一个对象、换行分隔），导入会直接报「格式错误」。
题库导入在 W2 关键路径上，不修就卡死。已改为 JSON Lines 并重跑验证。

---

## 需要你操作的两件事（卡住联调）

这两件我代劳不了，但做完就能在开发者工具里跑起来。

### 1. 填 AppID 与云环境 ID

- `project.config.json` 的 `appid`：`touristappid` → 你的真实 AppID
- `miniprogram/app.js` 的 `wx.cloud.init({ env })`：换成云环境 ID

### 2. 建库（约 20 分钟）

照 `database/import/导入说明.md` 一步步做：

1. 建 10 个集合
2. **每个集合的权限改为「仅管理端可读写」**——默认规则会让客户端绕过云函数直连数据库，题库可被拖走
3. 照 `database/indexes.md` 建 16 条索引（其中 2 条唯一索引承担业务约束，不能省）
4. 导入 `database/import/config.json`（5 条初始配置）和题库

> 云开发套餐必须**标准版**。个人版云函数超时锁死 3s、内存锁死 256MB，判题事务必然超时。

---

## 关键决策（已锁定，勿重复讨论）

| 项 | 结论 |
|---|---|
| 开发框架 | 原生小程序，不用 Taro/uni-app |
| 后端 | 微信云开发标准版 |
| V1 范围 | 不含限时模考，`pkg-exam` 分包位留给 V1.1 |
| Tab | 首页 / 题库 / 错题本 / 我的 |
| 判题 | 全在云端，下发题目不含 `answer` |
| 数据访问 | 全部走云函数，集合一律「仅管理端可读写」 |
| 状态管理 | `globalData` + 自研事件总线，不引 mobx |

---

## 上线关键路径

**备案（3~5 周）比开发（4 周）+ 提审（1 周）还长。** 上线时间由「第 1 天有没有提交备案」
决定，不由代码决定。备案已启动，开发必须跟上，否则备案下来了代码没好就白等。

备案流程里最容易翻车的一环：平台初审后会发**工信部 12381 短信核验**，
须在 **24 小时内**完成，超时自动驳回、申请作废需重来。安排专人盯这个短信。

---

## 主要文档

| 文件 | 内容 |
|---|---|
| `docs/架构设计.md` | 13 章完整架构 + 2 附录（**唯一权威架构文档**） |
| `docs/架构设计-v1.0-存档.md` | 初版，仅存档，勿参照 |
| `docs/上线提审清单.md` | 备案、类目、隐私指引、后台配置、时间线 |
| `docs/版本管理规范.md` | 分支策略、三套版本体系、云函数灰度 |
| `database/indexes.md` | 16 条索引清单 |
| `database/schema.md` | 集合字段结构 |
| `database/import/导入说明.md` | 建库四步 + JSON Lines 格式要求 |
