# 2026-08-29

## 行测小助手 · 微信小程序架构设计与工程骨架

从零开始搭建。用户诉求：快速上线并获取用户。

### 已确认的关键决策（用户拍板）

| 项 | 选择 |
|---|---|
| 技术栈 | 原生小程序 + 微信云开发（不用 Taro/uni-app） |
| V1.0 功能 | 专项刷题+即时解析、错题本+收藏、每日打卡+排行榜（**限时模考明确不进 V1**） |
| 题库来源 | 自有/自研题库，用户提供数据 |
| 主体 | 企业/个体工商户（可开微信支付） |

### 交付内容

80 个文件、约 4400 行代码：

- `docs/架构设计.md` — 11 章主文档（选型/分包/数据模型/云函数/性能/增长/合规/上线计划/风险）
- `miniprogram/` — 5 主包页 + pkg-rank 分包（checkin/rank/poster）+ question-card 组件 + utils + services
- `cloudfunctions/` — 9 个：login question answer checkin wrongbook favorite rank share timer track
- `database/schema.md` + `indexes.md`（16 条索引）
- `scripts/import-questions.js` — 题库清洗切片，已用 sample-questions.csv 验证通过
- `overview.md` — 交付清单

### 架构上值得记住的几个决策

1. **判题全在云端，下发的题目不含 `answer`** — 题库是唯一资产，从源头防拖库。
   「即时解析」用逐题提交（instant 模式）实现，答案在作答后才下发，安全性不变。
   `answer` 云函数按 items 数组长度自动适配 instant/batch 两种模式。

2. **事务只包「必须强一致」的部分** — 云开发事务有执行时间上限，20 题全量写入会产生 60+ 次操作。
   只有 wrong_book + users.stats 进事务；records（只增日志）和 questions.stat（难度校准）放事务外异步写。

3. **`utils/cloud.js` 是唯一出口** — 全项目禁止直接 wx.cloud.callFunction / wx.cloud.database。
   将来迁自建后端只改这一个文件。

4. **错题本列表放主包** — tabBar 的 pagePath 必须指向主包页面，四个 tab 都得在主包。

5. **`config.audit_mode` 审核开关** — 提审时云端改配置隐藏排行榜/邀请/奖励，不用重新发版。

6. **微信关系链能力已收紧** — getFriendCloudStorage 等非游戏类目基本拿不到好友数据，
   好友榜改为「分享链接绑定 inviteBy 自建邀请关系」。

### 待办 / 风险

- **ICP 备案是关键路径**，企业主体 3~20 个工作日，必须第一天启动
- 类目建议先选「工具 > 效率」降低门槛，稳定后再加「教育 > 在线教育」
- tabBar 图标（81×81 PNG）和默认头像占位图待补
- 榜单「击败百分比」当前是本地估算，V1.1 应换成云端真实分位

---

## 追加：版本管理体系建立（同日）

用户追问「怎么进行版本管理」。已初始化 git 仓库并建立完整规范。

### 交付

- `.gitignore` — 关键：`project.private.config.json` 必须忽略，否则团队天天解冲突
- `docs/版本管理规范.md` — 三套版本体系、分支策略、版本号规范、云函数热更、发布检查清单、常见坑
- `CHANGELOG.md`
- `miniprogram/utils/update.js` — 版本比较 / 静默更新 / 强制更新 / 柔性提示
- `app.js` 集成（延后 800ms 校验，避免弹窗被首页覆盖）

Git：`main`（线上，受保护）+ `develop`（体验版）+ `feature/*` + `hotfix/*`。
hotfix 必须同时合回 main 和 develop。当前在 develop。

### 核心认知

**小程序存在三套互不同步的版本体系**：
1. 代码版本（Git）— 即时
2. 平台版本（微信后台）— 1~7 工作日，**要审核**
3. 业务版本（config 集合 + 云函数）— **即时，且不需要审核**

**云函数是变相热更**：云开发支持版本 + 别名 + 流量分配，改云函数几分钟生效不用提审。
代价是必须向前兼容——小程序用户不会同时升级，云函数接口只加字段不删字段，废弃留 2 个版本周期。

### 踩到的坑（已修复）

`compareVersion('v1.2.3', '1.2.3')` 原本返回 -1。原因：'v1' 被 parseInt 解析成 NaN → 0。
Git tag 惯例带 v 前缀，而微信后台版本号是手填的，两者不一致会导致**强制更新静默失效**——
不报错，极难排查。已加 `normalize()` 去掉 v/V 前缀并 trim，18 个用例全通过。

---

## 追加：接入 GitHub（同日）

用户选择：**Public 公开仓库**、手动在网页建仓、暂不配 CI、git 作者用占位信息。

### 交付

- `scripts/setup-github.sh` — 加远端并推 main/develop，含参数校验与工作区检查，
  已验证无强推/reset --hard/rm -rf
- `README.md` — 项目简介、快速开始、目录结构
- `miniprogram/config.example.js` + `miniprogram/config.js`（不入库）
- `docs/版本管理规范.md` 新增「远端仓库（GitHub）」章节

### 关键改进：敏感配置分离

因为选了公开仓库，把真实云环境 ID 从 `app.js` 抽到 `miniprogram/config.js`（已 gitignore），
模板 `config.example.js` 入库。`app.js` 在 config.js 缺失时回退读模板占位值并 console.warn，
保证克隆下来能打开不白屏。

**原因**：不分离的话，用户填入真实环境 ID 后下次 commit 就会推到公开仓库。
同类思路也适用于 `project.config.json` 的 appid（可放 project.private.config.json）。

### 公开前已扫描确认

密钥/AppID/邮箱/手机号/IP — 全部未发现。appid 是 `touristappid` 占位，云环境是占位值。
题库正文不入库（走导入脚本），sample-questions.csv 只有 6 道示例题。

### 已明确告知用户会公开的内容

`docs/架构设计.md`（完整技术架构）、版本管理规范、全部业务代码。
如不希望对外披露，改 Private 即可（免费账号不限私有仓库数）。

### 待用户执行

1. GitHub 网页建**空**仓库（别勾 README/.gitignore/LICENSE）
2. `./scripts/setup-github.sh <仓库地址>`
3. 网页配置：默认分支改 develop、main 加分支保护、邀请协作者
4. 改 git 作者信息（当前占位 `xingce-dev` / `dev@example.com`）
5. `cp miniprogram/config.example.js miniprogram/config.js` 并填真实环境 ID
