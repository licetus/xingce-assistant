## 题目来源标识（真题 / AI 生成）✅ commit d2c6dae（2026-09-08 01:00~01:15）

- 需求：题目须向用户明示来源——真题或 AI 生成（生成功能 V1.1 加入）
- source 字段语义定稿：'real' 历年真题 | 'ai' AI生成 | 'self' 自研改编
- **线上 682 题批量回填 source:'real'**：MCP writeNoSqlDatabaseContent
  update 默认单文档（modifiedCount:1），**必须传 isMulti:true 才是批量更新**（681+1=682，0 残留）
- question.draw 投影 + sanitize 白名单下发 source；缺失兜底 'real'
- question-card 来源标签：真题（默认主色 tag）/ AI 生成（.tag-ai 警示橙），
  addGlobalClass:true 使 app.wxss 全局 .tag 生效、组件内 .tag-ai 覆盖背景色
- 合规沉淀进 schema.md：AI 生成内容不标识属平台审核红线，V1.1 AI 题必须标 source:'ai'
- question 已重部署；带守卫接口 MCP 冒烟 401（已知注入不稳定），以本地 152/152 为准
- CI 4/4 全绿

## 流量主广告接口基建 ✅ commit 92e6be9（2026-09-08 01:15~01:25）

- utils/ad.js：广告位 ID 走 config 热下发（ad_banner_home / ad_video，空 = 未启用，
  ID 必须 adunit- 前缀否则视为非法）；激励视频 showVideoAd() 封装：
  单例复用（重复 create 有配额坑）、换 ID 自动销毁重建、show 失败二次拉起、isEnded 完整观看判断
- 首页底部 Banner 接入：wx:if 控制渲染 + binderror 自动隐藏
- app.canShow blocked 加 'ad'：audit_mode=true 时全部广告关停（提审救命）
- config 集合已写入两条空基线，login.config 下发验证 ✅
- 合规红线（schema.md + 提审清单）：不诱导点击、不遮挡广告、看完才发奖励
- **用户手动：UV≥1000 后后台开通流量主 → 创建广告位 → 把 adunit-xxx 填进 config 即生效**
- 新增 8 单测（160/160），CI 4/4
- 坑：git commit -m 双引号 message 里含反引号会被 shell 命令替换，message 尽量避免反引号

## 广告说明文档 + 今日归档（01:30~01:40）

- 产出 docs/广告接入指南.md：开通条件（UV≥1000）、广告位创建步骤、config 填法、
  代码侧接口说明、合规红线、收益预期、排查 FAQ 七节，面向用户可直接照做
- 今日工作小结见下方归档

---

# 2026-09-08 归档小结

**主题**：V1 提审前的功能补全日（两项用户需求，全部交付）

1. **题目来源标识**（d2c6dae）：source 字段语义定稿 real/ai/self，线上 682 题
   批量回填 real，云函数下发 + 卡片标签（真题/AI 生成），AI 不标识属审核红线
2. **流量主广告基建**（92e6be9）：utils/ad.js + 首页 Banner + 激励视频封装，
   config 热下发（ad_banner_home/ad_video 空=未启用），audit_mode 一键关停
3. **广告说明文档**：docs/广告接入指南.md

**代码状态**：origin/develop = 92e6be9，npm test 160/160，CI 4/4 全绿
**提审前用户手动项**：真机预览（海报/小程序码/隐私弹窗/提醒字段）→ Excel 填 682 题解析
→ ICP 备案核验 → 提审时 audit_mode 置 true
**V1.1 候选**：AI 出题（source:'ai' + showVideoAd 解锁，链路已就绪）

## 开启时插屏广告（每日三时段频控）✅ commit a7ecc38（01:40~01:50）

- 需求：开启小程序时弹插屏，每日三次，以 12 点/18 点分界 → 实现为三时段各 1 次
- utils/ad.js：tryShowOpenInterstitial()，onShow + bootstrap 完成（config 就绪）双触发点；
  频控 key = ad_islot_{东八区日期}_{时段0/1/2}，存本地 cache（TTL 24h），跨天自动失效
- 细节：插屏实例展示后即失效每次新建；onLoad 后再 show；加载失败不消耗频控；8s 无填充放弃
- config.ad_interstitial 基线已入集合（空=未启用）；audit_mode 关停；纯前端改动无需部署云函数
- 测试坑：**mock-wx 的 storage Map 跨用例共享**，频控 key 污染后续用例 → beforeEach 必须 env.storage.clear()
- 167/167，推送 a7ecc38

## 插屏时段调整：晚上延长到凌晨 4 点 ✅ commit 8d4e663（01:55）

- 需求：晚上 18:00~次日 04:00，上午依次调整 → 上午 04:00-12:00 / 下午 12:00-18:00
- 关键设计：频控 key 改用「业务日」slotDayStr（时间戳回拨 4h 再取东八区日期），
  凌晨 0~4 点属前一业务日的晚上时段——23:50 与次日 01:30 共用同一频控锁，04:00 整翻新
- currentSlot：h∈[4,12)→0 / [12,18)→1 / 其余→2；新增 slotDayStr 导出
- 168/168；文档（指南×2 处、schema.md 去重 1 行）+ app.js 注释同步

## 插屏 3 小时全局冷却 ✅ commit 91ac154（02:05）

- 需求：3 小时内不弹第二次（跨时段生效）→ tryShowOpenInterstitial 头部加冷却检查
- 实现：cache key ad_ilast 存上次展示时间戳（TTL 3h 自动过期），now-last < 3h 拦截；
  展示成功才记录（失败不消耗冷却）；与时段频控叠加取更严者
- 边界：恰好满 3h 视为期满放行（< 判断）；测试坑：写边界用例时 17:31 属下午时段会被
  时段频控误拦，边界断言要选不受时段干扰的时间点（09:00→12:00 整）
- 170/170，推送 91ac154

## 「我的」页调试面板 ✅ commit 5c2b43b（02:15~02:25）

- 需求：mine 底部加调试模式，集中触达所有条件触发功能；用户补充：提审时也要关闭
- 显隐双保险：config.debug_mode===true（热开关）&& app.canShow('debug')；
  'debug' 已加入 canShow blocked 名单 → audit_mode=true 时即使忘关 debug_mode 也强制隐藏
- 面板：版本/环境、audit_mode、Banner/激励视频/插屏/订阅模板状态、插屏频控实时状态
  （SLOT_NAMES + fmtCooldown 在 JS 侧算好避免 WXML 格式化坑）、config 字段清单
- 六动作：模拟强更（update.checkVersion 第二参 debugVersion 绕过 release 守卫+柔性提示当日缓存）、
  柔性提示、插屏 force 展示（ad.js opts.force 跳过冷却/时段，启用门禁保留）、
  播激励视频、拉订阅授权、打开隐私协议
- 线上 config 已写入 debug_mode=true（用户开发期可见），提审时置 false（提审清单 11a 条目）
- 令牌坑：tokens.wxss 只有 --radius/--radius-sm/--radius-capsule，没有 --radius-lg/-md
- 新建 test/unit/update.test.js（4 用例，getAccountInfoSync 手动 mock）；177/177，推送 5c2b43b

---

# 2026-09-08 归档小结

**主题**：V1 提审前功能补全日（用户需求 5 项全部交付）+ 定时任务两个致命 bug 修复

## 交付清单（按时间序）
1. **订阅消息完整链路 + 两个致命修复**（8f45501）：timer 缺 TriggerName 映射导致定时任务从未跑过、
   线上 4 个旧触发器丢失（createFunctionTrigger 全量覆盖坑）；notify 发送侧 + 配额封顶
2. **题目来源标识**（d2c6dae）：682 题回填 real，云函数下发 + 卡片标签（真题/AI 生成）
3. **流量主广告基建**（92e6be9）：utils/ad.js + 首页 Banner + 激励视频封装，config 热下发
4. **广告文档**（b90741f）：docs/广告接入指南.md；开通门槛更正 UV 500（f47bd35 补类型总览表）
5. **插屏广告**（a7ecc38）：开启时触发，三时段频控；时段调整（8d4e663：晚上延至次日 04:00，
   业务日 04:00 翻新）；3h 全局冷却（91ac154）
6. **调试面板**（5c2b43b）：mine 底部，debug_mode 热开关 + audit_mode 双保险，
   六动作触达全部条件触发功能；提审清单 11a：提审前 debug_mode 置 false

## 代码状态
- origin/develop = 5c2b43b，npm test 177/177，CI 4/4 全绿
- 线上 config：12 条（含 debug_mode=true、checkin_tmpl_id、广告位×3 空、audit_mode=false）
- V1 代码侧全部就绪，仅剩用户手动项（见 MEMORY.md 待办）

## 今日工具坑沉淀
- MCP writeNoSqlDatabaseContent update 默认单文档，批量 isMulti:true
- mock-wx storage Map 跨用例共享，频控类测试 beforeEach 必须 clear
- git commit -m 含反引号会被 shell 命令替换
- tokens.wxss 无 --radius-lg/-md

## 真机测试反馈修复（53d3ad6，2026-09-08 上午）

用户真机反馈两个 bug，均已修复：

1. **题干与真题标签混排**：gkzhenti 导入管线把材料原文错映射到 subtype（资料分析 114 +
   言语 24 + 判断 15 = 153 题），卡片把 subtype 渲染成标签 pill 导致材料挤进标签区。
   用一次性云函数 tmp-fix-material 修复（跑完即删）：93 题材料并入 stem（材料+空行+题干）、
   5 题仅清标签、55 题材料正文源站缺失 → status=0 下架 + tags「缺材料待补」。
   备份 database/raw/subtype-pollution-backup-2026-09-08.json（raw 目录 gitignore 内）。
2. **海报生成失败**：根因 errCode 41030 invalid page——小程序未发布时
   wxacode.getUnlimited 传 page 被拒。share 增加空 page 回退重试（发布前默认跳主页）；
   顺修缓存分支返回相对路径当 fileID 的隐患（客户端 getTempFileURL 只认 cloud://）。
   poster 客户端加 tempFileURL 空守卫；question-card 标签加 max-width 截断防御。

测试 179/179（mock-sdk 新增一次性 openapi 结果队列，支持 41030→重试成功模拟）。
share 已重部署 force=true；MCP invokeFunction 复现 401（通道限制，以真机为准）。

**新坑沉淀**：
- MCP writeNoSqlDatabaseContent 不支持 MongoDB 管道更新（$concat 等），逐题变换走一次性云函数
- wx-server-sdk 分页拉取后边改边分会漏（更新使 where 失配），必须先全部读出再处理
- 未发布小程序 getUnlimited 只能传空 page（默认主页）；发布后恢复正常传参

---

# 原创解析批量生成（进行中，2026-09-08 下午起）

任务：为线上 627 题（status=1，55 题缺材料已下架）撰写原创解析并回填 analysis 字段，
analysisSource:'ai' 追溯。版权口径：全部原创撰写（AI 辅助起草），基于公开题干+官方答案
用自有表述推理成文，绝不复制粉笔/华图/中公解析；题目保留 source:'real'。

工作目录 database/analysis-work/：
- questions-all.json 全量 627 题（_id/qid/module/type/stem/options/answer）
- batches/batch-01~32.json（每批 20 题）
- out/batch-NN-analysis.json 产物：[{"qid":N,"analysis":"……"}]
- 完成后：合并校验（627 全覆盖）→ 临时云函数分批 update 回填 → 删临时函数 → schema.md 加 analysisSource → 提交

进度：batch-01~14 已完成（280/627 题，qid 10001~10295，跳 10227~10231 原为图形题空号段）。
写作规范：数量题逐步验算再下笔；图形推理/截面/拼合题用「题库暂未收录图形+解题思路+答案」诚实策略；
数量题 10272 题干丢失分数（乙丙完成剩余 2/3），按答案 20 天反推验证后解析中注明；
10168 负责团队计数是「分配数×甲负责数×乙负责数」乘积（45+80+45=170）不是加和。

## 原创解析批量生成进度（17:20 更新，进行中）

- 目标：线上 627 题（status=1）逐批写原创解析（AI 辅助起草自有表述，绝不复制第三方解析），
  回填时 analysisSource:'ai'，source 保持 'real'
- 目录：database/analysis-work/batches/batch-NN.json（32批×20题）→ out/batch-NN-analysis.json
- 进度：batch-01~25 已完成（**496/627 题，25/32 批**），剩 batch-26~32
- 验算过的数量题：10438（T=75k/w=3.5k/多33k，注意 Q2−Q1=9k 对应 0.12T 不是 0.3T）、
  10529（10份制：甲63乙51丙43丁53）、10533（甲专A乙专B 28天）、10537（圆锥1/8→23.5cm）、
  10536（DFS 无关：7天排2个连2天块+3个不相邻休息日，2/20=1/10）
- 图形/公式/图表依赖题统一诚实标注「题库暂未收录图片」，给解法框架+按官方答案定位（10435/10436/10530/10535/10538-10540/10548）
- 待办：剩余批次 → 合并校验（627 全覆盖）→ 临时云函数分批回填 → 删函数 → schema.md 补 analysisSource → 提交

## 原创解析回填完成（17:55 更新，✅ 已收尾）

- 回填方式：**云存储中转 + 一次性云函数下载全量处理**（比逐批 invoke 高效得多）
  - `manageStorage upload` 上传 merged-analysis.json → `manageFunctions createFunction`
    （temp-analysis-backfill，Nodejs18.15，timeout 300）→ `updateFunctionCode` 覆盖为
    「cloud.downloadFile(fileID) → JSON.parse → 10 条并发 where({qid}).update()」→
    `invokeFunction` 一次跑完：**627/627 updated，0 失败，仅 6.7s**
  - 教训：别用 invokeFunction 逐批传大 payload（13 次 × 50KB 纯浪费）；
    fileID 可从上传返回的 bucket 域名拼出：`cloud://<envId>.<bucket>/<cloudPath>`
- 线上验证：抽查 10001/10438/10619/10672 均已写入 analysis + analysisSource='ai'，
  source 仍 'real'；全库查询 `status=1 且 analysisSource!='ai'` 为 0 条 ✅
- 清理：临时云函数已 deleteFunction(confirm)，云存储文件已 delete(force)，本地函数目录已删
- 提交：develop `544e4fc`（69 files，含 batches/ + out/ + schema.md analysisSource 字段说明）
- 诚实标注口径保留：图形/材料依赖题解析内注明「题库暂未收录图片，请结合原题作答」，
  未编造任何数据；后续补齐材料图片后这些解析可按需人工复核

## 真机 bug 修复：选项无高亮（13:20，✅ commit 60caab4）

- 用户反馈「选择选项后没有高亮提示」
- **根因：WXML Mustache 不支持方法调用**——`chosen.indexOf(item.key)` 等在绑定里
  求值为空，`undefined >= 0` 恒 false → is-chosen/is-right/is-wrong 类名永远加不上。
  连带 `result.answer.join('')` 也渲染为空（正确答案字母缺失）
- 修复：question-card 组件 JS 侧预计算 `viewOptions`（isChosen/isRight/isWrong）+
  `answerText`，WXML 只做属性访问；observer 'q' 变化时重置 chosen/showAnalysis；
  observer 'result' 与 onTapOption/onConfirmMulti 后调 `_refreshOptions()`
- 全站 grep `\.w+\(` 确认仅 question-card 一处非法方法调用，无同类遗留
- npm test 179/179 ✅；develop 60caab4 已推送
- **教训（重要）：WXML 绑定只能用 三元/算术/逻辑/数据路径，indexOf/join/slice
  等一律要在 JS 侧算好再进 data；现有测试体系测不到 WXML 表达式求值，
  这类 bug 只能真机暴露——以后组件模板新增复杂绑定需人工过一遍**
- 待用户：微信开发者工具重新编译 → 真机预览验证（instant 模式点选应立即蓝色高亮，
  提交后正确绿/错误红 + 正确答案字母正常显示）

## 真题标签显示具体出处（13:55，✅ commit aa48e60）

- 用户需求「真题标签应当详细显示具体信息」
- **连带发现重大勘误**：fetch-gkzhenti.js 初版 paperId→年份/卷别映射 9 套错 8 套！
  核实依据：站点 h3/<title>（逐 paperId 重抓核对）+ paperId 创建时间戳
  （1735…≈2024-12→2025年度国考；1767…≈2026-01→2026年度）+ 数量关系15题=副省级特征。
  即 fetched-2025-*.json 实为 2026 卷、fetched-2026-*.json 实为 2025 卷，
  2024 的 dimenji/xingzheng 两文件卷别互换。对照表 database/raw/PAPER-MAPPING.md
- 新增 questions.exams 数组（['2024行政执法','2024地市级']），一题多卷如实多项
  （国考各卷共享题目，512 处跨卷撞题）；全量回填 682/682 ✅（stem 匹配 656 +
  qid 区块邻近推断 26，其中 16 道材料题 + 10 道边界下架题为推断，恢复材料时人工复核）
- tags 纠正：错误「国考YYYY卷别」→「国考YYYY」年份标签（卷别细节看 exams），
  业务标签（缺材料待补）保留
- 端上：question-card sourceLabel 在 JS 预计算（observer q），显示规则：
  单卷「2024国考·行政执法」/ 同年多卷「2024国考·执法/地市」/ 跨年「2024/2025国考」
- question + wrongbook 云函数已重部署（force=true，下发 exams）；
  临时函数 temp-exam-backfill 与云存储文件已删；npm test 179/179；develop aa48e60
- 教训：外部数据源的 ID→元信息映射必须用站点页面原始标题核实，不能凭文件命名想当然

## 头像渲染层加载失败修复（14:10，✅ commit 3632be3）

- 报错：Failed to load image http://127.0.0.1:19680/__pageframe__/pages/index/cloud://...
  （用户先前说「编译报错」，实为运行时渲染层图片加载错误，编译本身没问题）
- 根因：<image src> 直填 cloud:// fileID 在渲染层（工具 webview __pageframe__）转换
  不稳定，失败时 fileID 被当相对路径拼到页面路径后
- 修复：utils/cloud.js 新增 resolveFileUrls/resolveFileUrl（getTempFileURL 换 https
  临时链接 + Map 内存缓存 + 失败回退原 fileID）；index/mine/rank 三处头像统一转换，
  榜单批量一次换链；本地刚选的临时路径不转换
- **教训（沉淀约束）：凡 cloud:// fileID 进 <image src> 或 canvas drawImage，
  必须先 getTempFileURL 换链接，不要依赖组件对 fileID 的内置支持**
- npm test 179/179；develop 3632be3 已推送。待用户重新编译验证头像显示
